import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1c1c17',
      hover: '',
    },
    secondary: {
      main: '#3298cc',
    },
    background: {
      default: '#ffffff'
    }
  },
  typography: {
  },
});

export default theme